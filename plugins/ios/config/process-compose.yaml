version: "0.5"

processes:
  ios-simulator:
    command: "bash -lc '. \"{{ .Virtenv }}/scripts/simctl.sh\"; ios_service'"
    availability:
      restart: "on_failure"
    shutdown:
      signal: 15
      timeout: 120

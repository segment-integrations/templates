version: "0.5"

processes:
  android-emulator:
    command: "bash -lc '. \"{{ .Virtenv }}/scripts/avd.sh\"; android_service'"
    availability:
      restart: "on_failure"
    shutdown:
      signal: 15
      timeout: 120

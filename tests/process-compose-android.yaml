version: "0.5"

log_location: "test-results/android-repo-e2e-logs"
log_level: info

processes:
  # Test the Android plugin test suite from the example
  test-android-example:
    command: "cd examples/android && devbox run test:e2e"
    availability:
      restart: "no"
    readiness_probe:
      exec:
        command: "echo 'Test complete'"
      initial_delay_seconds: 1
      period_seconds: 5
      timeout_seconds: 600
      success_threshold: 1
